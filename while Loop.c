#include <stdio.h>

int main() {
    int i = 1;

    // Keep printing while i is less than or equal to 5
    while (i <= 5) {
        printf("Count: %d\n", i);
        i++;  // Increase i by 1
    }

    return 0;
}
